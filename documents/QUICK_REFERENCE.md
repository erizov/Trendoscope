# 🚀 Quick Reference - Trendoscope

## 📚 Как работает RAG Storage

### Структура хранения

```
data/
├── faiss_index.bin (60 KB)       → Vector embeddings (384d)
├── faiss_docs.json (31 MB)       → Полные тексты постов
├── style_guide.json (1.7 KB)    → Стиль автора
└── posts_metadata.json (1.3 KB) → Метаданные
```

### Что хранится

**FAISS Vector DB (RAG)**:
- Embeddings всех постов для semantic search
- Полные тексты в JSON
- Автозагрузка при запуске сервера
- Персистентность на диске

**Style Guide**:
- Характерные фразы автора (20+)
- Словарь и лексика
- Средняя длина постов
- Типичные теги и сентимент

---

## 🔧 Основные команды

### Проверить статус RAG

```bash
python check_rag.py
```

**Показывает**:
- Сколько постов в RAG
- Размер файлов
- Sample phrases и vocabulary
- Готовность к генерации

### Загрузить полный блог

```bash
# Загрузить civil-engineer (по умолчанию)
python load_full_blog.py

# Загрузить первые 500 постов
python load_full_blog.py --max-posts 500

# Загрузить другой блог
python load_full_blog.py --blog-url https://другой-блог.livejournal.com

# Загрузить ВСЕ посты (может быть долго)
python load_full_blog.py --max-posts 0
```

**Время**: ~10-20 минут для 500 постов

### Запустить сервер

```bash
python run.py
```

Откроется на http://localhost:8003

### Сгенерировать пост (CLI)

```bash
curl -X POST "http://localhost:8003/api/post/generate?style=philosophical&topic=ai&provider=openai" \
  -H "Content-Type: application/json" \
  -d "[]"
```

---

## 🎯 Генерация постов

### Через Web UI

1. **Открыть**: http://localhost:8003
2. **Прокрутить** к "Генератор постов в стиле автора"
3. **Выбрать тему**:
   - 🤖 AI
   - 🏛️ Politics
   - 🇺🇸 US Affairs
   - 🇷🇺 Russian History
   - 🔬 Science
4. **Выбрать стиль**:
   - Philosophical
   - Ironic
   - Analytical
   - Provocative
5. **Нажать** "Сгенерировать пост"

### Лучшие комбинации

| Тема | Стиль | Результат |
|------|-------|-----------|
| AI | Philosophical | Философское размышление о сознании |
| Politics | Ironic | Саркастический взгляд на геополитику |
| US Affairs | Analytical | Глубокий анализ американской политики |
| Russian History | Philosophical | Исторические параллели и уроки |
| Science | Analytical | Научные прорывы и их значение |

---

## 📊 RAG в деталях

### Как данные попадают в RAG

```python
# 1. Scraping
posts = scrape_livejournal(blog_url, max_posts=500)

# 2. NLP Analysis
analyzed = [analyze_text(p['text']) for p in posts]

# 3. Create Embeddings
model = SentenceTransformer('all-MiniLM-L6-v2')
embeddings = model.encode([p['text'] for p in analyzed])

# 4. Add to FAISS
store.add_documents(analyzed)  # Автосохранение!

# 5. Style Analysis
style = analyze_style(analyzed)
save_analysis_results(analyzed, style, blog_url)
```

### Как данные используются

```python
# При запуске сервера - автозагрузка
store = get_store()  # Загружает из data/faiss_index.bin

# При генерации поста
analyzed_posts = store.documents  # Уже в памяти!
similar = store.search("искусственный интеллект", top_k=5)

# Semantic search
results = store.search("тема поста", top_k=5)
# Возвращает 5 наиболее релевантных постов
```

---

## 🔍 Semantic Search примеры

```python
from trendascope.index.vector_db import get_store

store = get_store()

# Поиск по теме
results = store.search("искусственный интеллект", top_k=5)
for doc in results:
    print(f"{doc['title']} (score: {doc['score']:.2f})")

# Поиск похожих постов
results = store.search("история США и политика", top_k=3)

# Поиск по настроению
results = store.search("ироничные размышления о власти", top_k=5)
```

---

## 💡 Практические сценарии

### Сценарий 1: Первая настройка

```bash
# 1. Загрузить блог
python load_full_blog.py

# 2. Проверить
python check_rag.py

# 3. Запустить сервер
python run.py

# 4. Генерировать посты!
```

### Сценарий 2: Регулярное обновление

```bash
# Раз в месяц - загрузить новые посты
python load_full_blog.py --max-posts 50

# Проверить что добавилось
python check_rag.py
```

### Сценарий 3: Смена блога

```bash
# Загрузить другой блог
python load_full_blog.py --blog-url https://новый-блог.livejournal.com

# Теперь генерация будет в стиле нового блога
```

### Сценарий 4: Очистка и перезагрузка

```bash
# Удалить старые данные
rm data/*.bin data/*.json

# Загрузить заново
python load_full_blog.py
```

---

## 📖 Документация

| Файл | Содержание |
|------|-----------|
| `README.md` | Главная документация |
| `QUICKSTART.md` | Быстрый старт |
| `RAG_STORAGE_GUIDE.md` | **Полное руководство по RAG** |
| `TOPIC_FOCUS_GUIDE.md` | Гайд по темам генерации |
| `POST_GENERATOR_GUIDE.md` | Генерация постов |
| `CHANGELOG.md` | История изменений |
| `this file` | Быстрая справка |

---

## 🆘 Troubleshooting

### "Style guide not found"

```bash
python load_full_blog.py
```

### "Vector DB empty"

```bash
python check_rag.py  # Проверить статус
python load_full_blog.py  # Загрузить данные
```

### "Сервер не запускается"

```bash
# Проверить порт
netstat -ano | findstr :8003

# Изменить порт в run.py
# port=8004
```

### "Генерация не работает"

1. Проверить RAG: `python check_rag.py`
2. Проверить API ключ в `.env`
3. Перезапустить сервер: `python run.py`

---

## 🎓 Понятия

**RAG** - Retrieval-Augmented Generation  
Поиск релевантной информации → генерация с контекстом

**FAISS** - Facebook AI Similarity Search  
Быстрый поиск похожих векторов (< 100ms)

**Embeddings** - Векторное представление текста  
384-мерные вектора для semantic search

**Style Guide** - Анализ стиля автора  
Фразы, лексика, сентимент, структура

**Semantic Search** - Поиск по смыслу  
Не точное совпадение, а семантическая близость

---

## ⚡ Быстрые ответы

**Q: Сколько постов нужно?**  
A: Минимум 30, оптимально 100-300

**Q: Как часто обновлять?**  
A: Раз в месяц или при новых постах

**Q: Размер хранилища?**  
A: ~50 KB на пост (embeddings + text)

**Q: Скорость поиска?**  
A: < 100ms для 1000 постов

**Q: Можно ли несколько блогов?**  
A: Да, загрузите их последовательно

**Q: Работает офлайн?**  
A: Да, после первой загрузки

---

## 🔗 Ссылки

- **Web UI**: http://localhost:8003
- **API Docs**: http://localhost:8003/docs
- **Data Directory**: `E:\Python\FastAPI\Trendoscope\trendascope\data`

---

**Version**: 2.1.0  
**Last Updated**: 2025-11-12  
**RAG Backend**: FAISS + sentence-transformers

