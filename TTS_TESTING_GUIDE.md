# 🧪 TTS Testing Guide

## 🚀 Quick Start & Testing

### Step 1: Check Dependencies

Проверьте, установлены ли зависимости:

```bash
cd trendoscope2
pip list | grep -E "gtts|pydub"
```

Если не установлены, установите:

```bash
pip install gtts>=2.5.0 pydub>=0.25.1
```

**Или установите все зависимости:**
```bash
pip install -r requirements.txt
```

### Step 2: Run Application

```bash
python run.py
```

Приложение запустится на: `http://localhost:8004`

### Step 3: Test TTS API

#### Test 1: Generate TTS (Russian)

```bash
curl -X POST http://localhost:8004/api/tts/generate \
  -H "Content-Type: application/json" \
  -d '{
    "text": "Привет, это тестовое сообщение на русском языке.",
    "language": "ru",
    "voice_gender": "female"
  }'
```

**Ожидаемый результат:**
```json
{
  "success": true,
  "audio_id": "uuid-here",
  "audio_url": "/api/tts/audio/uuid-here",
  "language": "ru",
  "duration": 2.5,
  "created_at": "2025-12-16T..."
}
```

#### Test 2: Generate TTS (English)

```bash
curl -X POST http://localhost:8004/api/tts/generate \
  -H "Content-Type: application/json" \
  -d '{
    "text": "Hello, this is a test message in English.",
    "language": "en",
    "voice_gender": "female"
  }'
```

#### Test 3: Auto Language Detection

```bash
curl -X POST http://localhost:8004/api/tts/generate \
  -H "Content-Type: application/json" \
  -d '{
    "text": "Привет! Hello! This is mixed text.",
    "language": "auto",
    "voice_gender": "female"
  }'
```

#### Test 4: Get Audio File

```bash
# Используйте audio_id из предыдущего ответа
curl http://localhost:8004/api/tts/audio/{audio_id} --output test_audio.mp3
```

Проверьте, что файл создан и воспроизводится:
```bash
# Windows
start test_audio.mp3

# Linux/Mac
open test_audio.mp3
```

### Step 4: Test Frontend

1. Откройте браузер: `http://localhost:8004`
2. Дождитесь загрузки новостей
3. Нажмите кнопку **"🔊 Читать вслух"** на любой новости
4. Проверьте:
   - ✅ Модальное окно открывается
   - ✅ Аватар отображается
   - ✅ Аудио генерируется (статус "Генерация аудио...")
   - ✅ Кнопка Play работает
   - ✅ Аватар анимируется при чтении
   - ✅ Прогресс-бар обновляется
   - ✅ Скорость регулируется

### Step 5: Test Caching

1. Сгенерируйте TTS для одного и того же текста дважды
2. Первый раз: должно занять 2-5 секунд
3. Второй раз: должно быть мгновенно (из кэша)

Проверьте кэш:
```bash
# Windows PowerShell
dir trendoscope2\data\audio\tts\cache

# Linux/Mac
ls trendoscope2/data/audio/tts/cache
```

## 🐛 Troubleshooting

### Проблема: "ModuleNotFoundError: No module named 'gtts'"

**Решение:**
```bash
pip install gtts>=2.5.0
```

### Проблема: "ModuleNotFoundError: No module named 'pydub'"

**Решение:**
```bash
pip install pydub>=0.25.1
```

**Примечание:** pydub требует ffmpeg для некоторых операций. Если возникнут проблемы:
- Windows: Скачайте ffmpeg и добавьте в PATH
- Linux: `sudo apt-get install ffmpeg`
- Mac: `brew install ffmpeg`

### Проблема: "TTS generation failed"

**Возможные причины:**
1. Нет интернета (gTTS требует интернет)
2. Текст слишком длинный (максимум 5000 символов)
3. Проблемы с сетью Google

**Решение:**
- Проверьте интернет-соединение
- Укоротите текст
- Проверьте логи сервера

### Проблема: Аватар не анимируется

**Проверьте:**
1. Откройте консоль браузера (F12)
2. Проверьте ошибки JavaScript
3. Убедитесь, что аудио загружается
4. Проверьте, что CSS загружен

### Проблема: Аудио не воспроизводится

**Проверьте:**
1. Консоль браузера на ошибки
2. Сеть (Network tab) - загружается ли аудио файл
3. Формат файла (должен быть MP3)

## ✅ Testing Checklist

### Backend Tests
- [ ] TTS генерирует аудио для русского текста
- [ ] TTS генерирует аудио для английского текста
- [ ] Автоопределение языка работает
- [ ] Кэширование работает
- [ ] API возвращает правильные ответы
- [ ] Аудио файлы сохраняются

### Frontend Tests
- [ ] Кнопка "Читать вслух" отображается
- [ ] Модальное окно открывается
- [ ] Аватар отображается
- [ ] Аудио генерируется
- [ ] Play/Pause работает
- [ ] Скорость регулируется
- [ ] Прогресс-бар обновляется
- [ ] Анимация губ работает
- [ ] Выбор языка работает

### Integration Tests
- [ ] Интеграция с новостной лентой работает
- [ ] Кнопка в модальном окне работает
- [ ] Ошибки обрабатываются корректно

## 📊 Performance Benchmarks

### Expected Performance

- **First generation:** 2-5 seconds
- **Cached generation:** < 1 second
- **Audio file size:** ~50-200 KB per minute
- **Max text length:** 5000 characters

### Test Performance

```bash
# Test generation time
time curl -X POST http://localhost:8004/api/tts/generate \
  -H "Content-Type: application/json" \
  -d '{"text": "Test message", "language": "en"}'
```

## 🎯 Next Steps After Testing

После успешного тестирования MVP:
1. ✅ Убедитесь, что все тесты пройдены
2. ⏭️ Переходите к Фазе 2 (кэширование + офлайн режим)

---

**Готово к тестированию!** 🚀
