# 🔍 RSS Sources Testing Guide

## Обзор

Скрипт `test_rss_sources.ps1` проверяет все RSS-источники на работоспособность перед добавлением в список.

## 🚀 Быстрый запуск

```powershell
cd trendoscope2
.\scripts\test_rss_sources.ps1
```

## 📊 Что делает скрипт

1. **Загружает все RSS-источники** из `NewsAggregator`
2. **Проверяет каждый URL** на:
   - Доступность (200 OK)
   - Формат (XML/RSS)
   - Редиректы (301/302)
   - Ошибки (404, 403, и др.)
3. **Группирует результаты** по статусам
4. **Сохраняет детальный отчет** в `rss_test_results.json`

## 📋 Результаты

### Статусы

- ✅ **OK** - Источник работает корректно
- → **REDIRECT** - Редирект (нужно обновить URL)
- ✗ **NOT_FOUND (404)** - Источник не найден (удалить из списка)
- ✗ **FORBIDDEN (403)** - Доступ запрещен (может требовать аутентификацию)
- ⚠ **WARNING** - Работает, но не XML/RSS формат
- ✗ **ERROR** - Другие ошибки

### Пример вывода

```
========================================
Summary
========================================

Total sources tested: 45

✓ Working: 38
→ Redirects: 3
✗ Not Found (404): 2
✗ Forbidden (403): 1
⚠ Warnings: 0
✗ Errors: 1

Success rate: 84.4%
```

## 🔧 Использование результатов

### 1. Проверка редиректов

Скрипт покажет новые URL для редиректов:
```
Redirects (need to fix URLs):
  https://old-url.com/rss
    → https://new-url.com/rss
```

Обновите URL в `news_sources.py`.

### 2. Удаление неработающих источников

Скрипт покажет источники с 404:
```
Not Found (404) - Remove from list:
  https://example.com/rss (RUSSIAN_SOURCES)
```

Удалите их из соответствующего списка в `news_sources.py`.

### 3. Проверка детального отчета

Откройте `rss_test_results.json` для детальной информации:
```json
{
  "OK": [
    {
      "Category": "RUSSIAN_SOURCES",
      "Url": "https://lenta.ru/rss",
      "Size": 12345
    }
  ],
  "NOT_FOUND": [
    {
      "Category": "RUSSIAN_SOURCES",
      "Url": "https://example.com/rss"
    }
  ]
}
```

## 📝 Рекомендации

### Перед добавлением нового источника

1. **Запустите тест:**
   ```powershell
   .\scripts\test_rss_sources.ps1
   ```

2. **Проверьте результаты:**
   - Убедитесь, что новый источник в статусе OK
   - Проверьте размер ответа (должен быть > 0)

3. **Добавьте в код:**
   - Добавьте URL в соответствующий список в `news_sources.py`
   - Запустите тест снова для проверки

### Регулярное обслуживание

Рекомендуется запускать тест:
- **Еженедельно** - для проверки работоспособности
- **Перед релизом** - для гарантии качества
- **После изменений** - для проверки новых источников

## 🐛 Troubleshooting

### Проблема: Скрипт не запускается

**Решение:**
```powershell
# Проверьте Python
python --version

# Проверьте зависимости
pip install httpx feedparser beautifulsoup4
```

### Проблема: Много таймаутов

**Решение:**
- Увеличьте таймаут в скрипте (по умолчанию 10 секунд)
- Проверьте интернет-соединение
- Некоторые источники могут быть временно недоступны

### Проблема: False positives (ложные срабатывания)

**Решение:**
- Некоторые источники могут блокировать автоматические запросы
- Проверьте URL вручную в браузере
- Используйте User-Agent заголовок (можно добавить в скрипт)

## 📚 Дополнительная информация

- **Скрипт:** `scripts/test_rss_sources.ps1`
- **Результаты:** `rss_test_results.json`
- **Источники:** `src/trendoscope2/ingest/news_sources.py`

---

**Важно:** Всегда тестируйте источники перед добавлением в продакшн!
