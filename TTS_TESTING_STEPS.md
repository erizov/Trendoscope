# 🧪 TTS Testing Steps - Пошаговое руководство

## 📋 Подготовка

### Step 1: Установка зависимостей

```bash
cd trendoscope2
pip install -r requirements.txt
```

**Проверка установки:**
```bash
pip list | findstr "gtts pydub pyttsx3"
```

Должны быть установлены:
- `gtts>=2.5.0`
- `pydub>=0.25.1`
- `pyttsx3>=2.90`

### Step 2: Запуск приложения

```bash
python run.py
```

**Проверка:**
- Откройте: `http://localhost:8004`
- Должна открыться новостная лента

---

## 🧪 Тестирование Backend (API)

### Test 1: Проверка Health Endpoint

```bash
curl http://localhost:8004/health
```

**Ожидаемый результат:**
```json
{
  "status": "healthy",
  "redis": "ok",
  "database": "ok"
}
```

### Test 2: Генерация TTS (Русский)

```bash
curl -X POST http://localhost:8004/api/tts/generate ^
  -H "Content-Type: application/json" ^
  -d "{\"text\": \"Привет, это тестовое сообщение на русском языке.\", \"language\": \"ru\", \"voice_gender\": \"female\"}"
```

**Ожидаемый результат:**
```json
{
  "success": true,
  "audio_id": "uuid-here",
  "audio_url": "/api/tts/audio/uuid-here",
  "language": "ru",
  "duration": 2.5,
  "provider": "gtts",
  "used_fallback": false,
  "created_at": "2025-12-16T..."
}
```

**Проверьте:**
- ✅ `success: true`
- ✅ `audio_id` присутствует
- ✅ `language: "ru"`
- ✅ `provider: "gtts"` (или "pyttsx3" если нет интернета)

### Test 3: Генерация TTS (Английский)

```bash
curl -X POST http://localhost:8004/api/tts/generate ^
  -H "Content-Type: application/json" ^
  -d "{\"text\": \"Hello, this is a test message in English.\", \"language\": \"en\", \"voice_gender\": \"female\"}"
```

### Test 4: Автоопределение языка

```bash
curl -X POST http://localhost:8004/api/tts/generate ^
  -H "Content-Type: application/json" ^
  -d "{\"text\": \"Привет! Hello! This is mixed text.\", \"language\": \"auto\", \"voice_gender\": \"female\"}"
```

### Test 5: Получение аудио файла

```bash
# Используйте audio_id из предыдущего ответа
curl http://localhost:8004/api/tts/audio/{audio_id} --output test_audio.mp3
```

**Проверка:**
- Файл `test_audio.mp3` создан
- Размер файла > 0
- Файл воспроизводится

```bash
# Windows
start test_audio.mp3
```

### Test 6: Офлайн режим (pyttsx3)

```bash
curl -X POST http://localhost:8004/api/tts/generate ^
  -H "Content-Type: application/json" ^
  -d "{\"text\": \"Test offline mode\", \"provider\": \"pyttsx3\", \"voice_gender\": \"female\"}"
```

**Проверьте:**
- ✅ `provider: "pyttsx3"`
- ✅ Аудио сгенерировано (даже без интернета)

### Test 7: Автоматический Fallback

**Вариант A: Отключить интернет**
1. Отключите интернет
2. Выполните:
```bash
curl -X POST http://localhost:8004/api/tts/generate ^
  -H "Content-Type: application/json" ^
  -d "{\"text\": \"Test fallback\", \"provider\": \"auto\"}"
```

**Проверьте:**
- ✅ `provider: "pyttsx3"`
- ✅ `used_fallback: true`

**Вариант B: Принудительно использовать gtts (должен упасть без интернета)**
```bash
curl -X POST http://localhost:8004/api/tts/generate ^
  -H "Content-Type: application/json" ^
  -d "{\"text\": \"Test\", \"provider\": \"gtts\"}"
```

### Test 8: Выбор голоса (pyttsx3)

```bash
# Мужской голос
curl -X POST http://localhost:8004/api/tts/generate ^
  -H "Content-Type: application/json" ^
  -d "{\"text\": \"Test male voice\", \"provider\": \"pyttsx3\", \"voice_gender\": \"male\"}"

# Женский голос
curl -X POST http://localhost:8004/api/tts/generate ^
  -H "Content-Type: application/json" ^
  -d "{\"text\": \"Test female voice\", \"provider\": \"pyttsx3\", \"voice_gender\": \"female\"}"
```

### Test 9: Кэширование

```bash
# Первый запрос (должен занять 2-5 секунд)
curl -X POST http://localhost:8004/api/tts/generate ^
  -H "Content-Type: application/json" ^
  -d "{\"text\": \"This is a test for caching.\", \"language\": \"en\"}"

# Второй запрос с тем же текстом (должен быть мгновенным)
curl -X POST http://localhost:8004/api/tts/generate ^
  -H "Content-Type: application/json" ^
  -d "{\"text\": \"This is a test for caching.\", \"language\": \"en\"}"
```

**Проверьте:**
- Первый: 2-5 секунд
- Второй: < 1 секунда

### Test 10: Статистика кэша

```bash
curl http://localhost:8004/api/tts/stats
```

**Ожидаемый результат:**
```json
{
  "success": true,
  "cache_enabled": true,
  "cache_dir": "path/to/cache",
  "cache_files": 5,
  "cache_size_bytes": 1024000,
  "main_files": 10,
  "main_size_bytes": 2048000
}
```

---

## 🎨 Тестирование Frontend

### Test 11: Открытие новостной ленты

1. Откройте: `http://localhost:8004`
2. Дождитесь загрузки новостей
3. Проверьте:
   - ✅ Новости отображаются
   - ✅ Кнопка "🔊 Читать вслух" видна на каждой карточке

### Test 12: Генерация TTS через UI

1. Нажмите "🔊 Читать вслух" на любой новости
2. Проверьте:
   - ✅ Модальное окно открывается
   - ✅ Аватар отображается
   - ✅ Статус: "Генерация аудио..."
   - ✅ Через 2-5 секунд: "Готово к воспроизведению"

### Test 13: Воспроизведение аудио

1. После генерации нажмите кнопку Play (▶)
2. Проверьте:
   - ✅ Аудио воспроизводится
   - ✅ Аватар анимируется (рот двигается)
   - ✅ Прогресс-бар обновляется
   - ✅ Время отображается корректно

### Test 14: Управление воспроизведением

1. Нажмите Pause (⏸)
2. Проверьте:
   - ✅ Аудио останавливается
   - ✅ Анимация останавливается
   - ✅ Кнопка меняется на Play

3. Нажмите Play снова
4. Проверьте:
   - ✅ Воспроизведение продолжается

### Test 15: Регулировка скорости

1. Измените скорость (0.5x - 2x)
2. Проверьте:
   - ✅ Скорость меняется
   - ✅ Метка скорости обновляется
   - ✅ Аудио воспроизводится с новой скоростью

### Test 16: Выбор языка

1. Измените язык в выпадающем списке
2. Проверьте:
   - ✅ Язык меняется
   - ✅ При следующей генерации используется выбранный язык

### Test 17: Кнопка в модальном окне

1. Откройте новость (нажмите "📖 Читать")
2. В модальном окне нажмите "🔊 Читать вслух"
3. Проверьте:
   - ✅ Открывается модальное окно аватара
   - ✅ Текст из модального окна используется

---

## 🐛 Troubleshooting Tests

### Проблема: API не отвечает

**Решение:**
1. Проверьте, что приложение запущено: `python run.py`
2. Проверьте порт: `http://localhost:8004`
3. Проверьте логи на ошибки

### Проблема: TTS не генерируется

**Проверьте:**
1. Интернет-соединение (для gTTS)
2. Установлен ли pyttsx3: `pip list | findstr pyttsx3`
3. Логи сервера на ошибки

### Проблема: Аватар не анимируется

**Проверьте:**
1. Консоль браузера (F12) на ошибки JavaScript
2. Загружается ли аудио файл (Network tab)
3. CSS загружен корректно

### Проблема: pyttsx3 не работает

**Windows:**
- ✅ Должно работать из коробки (SAPI5)

**Linux:**
- Установите: `sudo apt-get install espeak`

**macOS:**
- ✅ Должно работать из коробки

---

## ✅ Checklist

### Backend
- [ ] Health endpoint работает
- [ ] TTS генерирует аудио (русский)
- [ ] TTS генерирует аудио (английский)
- [ ] Автоопределение языка работает
- [ ] Аудио файл загружается
- [ ] Офлайн режим работает (pyttsx3)
- [ ] Fallback работает
- [ ] Выбор голоса работает
- [ ] Кэширование работает
- [ ] Статистика доступна

### Frontend
- [ ] Новостная лента открывается
- [ ] Кнопка "Читать вслух" видна
- [ ] Модальное окно открывается
- [ ] Аватар отображается
- [ ] Аудио генерируется
- [ ] Play/Pause работает
- [ ] Скорость регулируется
- [ ] Прогресс-бар обновляется
- [ ] Анимация работает
- [ ] Выбор языка работает

---

## 🚀 Запуск E2E тестов

```bash
# Запустить все e2e тесты
pytest tests/e2e/test_tts.py -v

# Запустить только TTS тесты
pytest tests/e2e/test_tts.py::TestTTSGeneration -v

# Запустить с выводом
pytest tests/e2e/test_tts.py -v -s
```

---

**Готово к тестированию!** 🎉
