# 🚀 Быстрый старт Трендоскоп

## 1. Установка зависимостей

```bash
cd trendascope
pip install -r requirements.txt
```

## 2. Запуск демонстрации

Самый быстрый способ увидеть систему в действии:

```bash
python demo.py
```

Это запустит полный pipeline на тестовых данных и покажет все возможности системы.

## 3. Запуск Web UI

```bash
python run.py
```

Затем откройте в браузере: **http://localhost:8000**

## 4. Использование API

### Через Web UI

1. Откройте http://localhost:8000
2. Введите URL блога: `https://civil-engineer.livejournal.com`
3. Выберите количество постов: `20`
4. Выберите стиль: `Логосфера` или другой
5. Выберите провайдер: `Demo` (для тестирования без API ключей)
6. Нажмите "Запустить анализ"

### Через cURL

```bash
curl "http://localhost:8000/api/pipeline/run?blog_url=https://civil-engineer.livejournal.com&max_posts=20&mode=analytical&provider=demo"
```

### Через Python

```python
from src.trendascope.pipeline.orchestrator import Pipeline

pipeline = Pipeline()
result = pipeline.run_full_pipeline(
    blog_url="https://civil-engineer.livejournal.com",
    max_posts=20,
    mode="analytical",
    provider="demo"
)

print(f"Найдено постов: {result['stats']['total_posts']}")
print(f"Трендов: {len(result['trends'])}")
print(f"Резюме: {result['generated']['summary'][:200]}...")
```

## 5. Настройка реальных LLM

### OpenAI

```bash
# В файле .env
OPENAI_API_KEY=sk-...
```

Затем в Web UI выберите провайдер `OpenAI`.

### Anthropic

```bash
# В файле .env
ANTHROPIC_API_KEY=sk-ant-...
```

Затем выберите провайдер `Anthropic`.

### Local (Ollama)

```bash
# Установите Ollama: https://ollama.ai
ollama pull llama2

# В файле .env (опционально)
OLLAMA_URL=http://localhost:11434
```

Затем выберите провайдер `Local`.

## 6. Запуск тестов

```bash
# Все тесты
pytest

# С подробным выводом
pytest -v

# Только интеграционные
pytest tests/test_pipeline.py

# Демо-тест
python tests/test_pipeline.py
```

## 7. Структура результатов

После запуска pipeline вы получите:

```json
{
  "posts": [...],          // Проанализированные посты
  "trends": [...],         // Трендовые темы
  "generated": {           // Сгенерированный контент
    "summary": "...",
    "titles": [...],
    "ideas": [...],
    "leads": {...},
    "viral_potential": {...}
  },
  "stats": {...}           // Статистика
}
```

## 🎨 Доступные стили

- **logospheric** - Краткие тезисы, точные цитаты
- **analytical** - Аналитический, для экспертных платформ
- **provocative** - Провокационный, вызывает дискуссию
- **humorous** - Юмористический, лёгкий
- **philosophical** - Философский, созерцательный
- **journalistic** - Журналистский, структурированный

## 🔧 Настройка для конкретного блога

Отредактируйте параметры в Web UI или коде:

```python
result = pipeline.run_full_pipeline(
    blog_url="https://YOUR-BLOG.livejournal.com",  # Ваш блог
    max_posts=50,                                    # Количество постов
    mode="analytical",                               # Стиль
    provider="openai",                               # Провайдер
    model="gpt-4-turbo-preview"                     # Модель
)
```

## 📚 Дополнительная информация

- Полная документация: `README.md`
- Описание архитектуры: `idea.txt`
- API документация: http://localhost:8000/docs (после запуска)

## ❓ Частые вопросы

**Q: Нужны ли API ключи для тестирования?**  
A: Нет, используйте провайдер `demo` для тестирования без API ключей.

**Q: Как анализировать приватные посты?**  
A: Укажите `LJ_USERNAME` и `LJ_PASSWORD` в `.env` файле.

**Q: Поддерживаются ли другие платформы кроме LiveJournal?**  
A: Пока нет, но архитектура позволяет легко добавить другие источники.

**Q: Как ускорить анализ?**  
A: Уменьшите `max_posts` или используйте Redis для кеширования.

## 🐛 Решение проблем

**Ошибка импорта модулей:**
```bash
# Убедитесь что находитесь в правильной директории
cd trendascope
export PYTHONPATH="${PYTHONPATH}:$(pwd)/src"
```

**Ошибка с векторной БД:**
```bash
# Установите FAISS
pip install faiss-cpu
```

**Ошибка с spaCy:**
```bash
# Опционально, но рекомендуется
python -m spacy download ru_core_news_sm
```

## 💬 Поддержка

При возникновении проблем проверьте:
1. Логи сервера
2. Консоль браузера (F12)
3. Результаты тестов: `pytest -v`

