# ⚡ TTS Testing - Quick Start

## 🚀 Самый быстрый способ протестировать

### Вариант 1: Автоматический скрипт (Рекомендуется)

```powershell
# Запустите скрипт (он проверит API и запустит тесты)
cd trendoscope2
.\scripts\test_tts.ps1
```

**Что делает скрипт:**
1. ✅ Проверяет, запущен ли API
2. ✅ Ждет запуска API (если нужно)
3. ✅ Запускает быстрые API тесты
4. ✅ Запускает E2E тесты через pytest

### Вариант 2: Запуск API + Тесты

```powershell
# Запускает API в фоне и тесты
.\scripts\start_and_test_tts.ps1
```

---

## 📋 Ручной запуск (если скрипты не работают)

### Step 1: Запустите API

**Терминал 1:**
```bash
cd trendoscope2
python run.py
```

Дождитесь сообщения: `Application startup complete`

### Step 2: Запустите тесты

**Терминал 2:**
```bash
cd trendoscope2
pytest tests/e2e/test_tts.py -v
```

---

## ✅ Быстрая проверка (30 секунд)

### PowerShell команды:

```powershell
# 1. Проверка API
Invoke-RestMethod -Uri "http://localhost:8004/health"

# 2. Генерация TTS
$response = Invoke-RestMethod -Uri "http://localhost:8004/api/tts/generate" `
  -Method POST -ContentType "application/json" `
  -Body '{"text": "Привет, это тест.", "language": "ru"}'

# 3. Проверка результата
Write-Host "Audio ID: $($response.audio_id)"
Write-Host "Provider: $($response.provider)"
```

---

## 🐛 Если тесты пропускаются (SKIPPED)

**Причина:** API не запущен или недоступен

**Решение:**
1. Запустите API: `python run.py`
2. Дождитесь запуска (5-10 секунд)
3. Запустите тесты снова: `pytest tests/e2e/test_tts.py -v`

---

## 📊 Ожидаемый результат

После успешного запуска вы увидите:

```
tests/e2e/test_tts.py::TestTTSGeneration::test_tts_generate_russian PASSED
tests/e2e/test_tts.py::TestTTSGeneration::test_tts_generate_english PASSED
tests/e2e/test_tts.py::TestTTSProviders::test_tts_provider_gtts PASSED
...
```

---

## 🎯 Минимальный тест Frontend

1. Откройте: `http://localhost:8004`
2. Нажмите "🔊 Читать вслух"
3. Проверьте работу аватара

---

**Готово!** 🎉
