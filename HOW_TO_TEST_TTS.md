# 🧪 Как протестировать TTS - Инструкция

## ⚡ Самый быстрый способ

### Вариант 1: Автоматический запуск API + тесты (Рекомендуется)

```powershell
cd trendoscope2
.\scripts\start_and_test_tts.ps1
```

**Скрипт автоматически:**
- ✅ Запустит API в фоне (если не запущен)
- ✅ Подождет запуска API
- ✅ Запустит быстрые API тесты
- ✅ Запустит E2E тесты

### Вариант 2: Ручной запуск API

```powershell
cd trendoscope2
.\scripts\test_tts.ps1
```

**Скрипт:**
- ✅ Предложит автоматически запустить API (y/n)
- ✅ Или подождет, если API уже запущен
- ✅ Запустит быстрые API тесты
- ✅ Запустит E2E тесты

---

## 🔍 Диагностика проблем

### Проверка состояния API

```powershell
.\scripts\check_api.ps1
```

Этот скрипт быстро проверит, запущен ли API.

### Если API не запускается автоматически

1. **Проверьте логи:**
   - Скрипт создает файл `api_startup.log` с выводом API
   - Проверьте последние строки для ошибок

2. **Запустите API вручную для диагностики:**
   ```bash
   python run.py
   ```
   Это покажет все ошибки в консоли.

3. **Проверьте зависимости:**
   ```bash
   pip install -r requirements.txt
   ```

---

## 📋 Если скрипт не работает

### Вариант A: Ручной запуск

**Шаг 1: Запустите API**
```bash
# Терминал 1
cd trendoscope2
python run.py
```

**Шаг 2: Запустите тесты**
```bash
# Терминал 2 (новый терминал)
cd trendoscope2
pytest tests/e2e/test_tts.py -v
```

### Вариант B: Проверка вручную

**1. Проверка API:**
```powershell
Invoke-RestMethod -Uri "http://localhost:8004/health"
```

**2. Генерация TTS:**
```powershell
$r = Invoke-RestMethod -Uri "http://localhost:8004/api/tts/generate" `
  -Method POST -ContentType "application/json" `
  -Body '{"text": "Привет", "language": "ru"}'
Write-Host "Audio ID: $($r.audio_id)"
```

**3. Откройте браузер:**
```powershell
start http://localhost:8004
```

---

## ✅ Ожидаемый результат

### При успешном запуске тестов:

```
tests/e2e/test_tts.py::TestTTSGeneration::test_tts_generate_russian PASSED
tests/e2e/test_tts.py::TestTTSGeneration::test_tts_generate_english PASSED
tests/e2e/test_tts.py::TestTTSProviders::test_tts_provider_gtts PASSED
...
```

### Если тесты SKIPPED:

**Причина:** API не запущен

**Решение:**
1. Запустите API: `python run.py`
2. Дождитесь запуска (5-10 секунд)
3. Запустите тесты снова

---

## 🎯 Минимальная проверка (30 секунд)

```powershell
# 1. Проверка
Invoke-RestMethod -Uri "http://localhost:8004/health"

# 2. Генерация
$r = Invoke-RestMethod -Uri "http://localhost:8004/api/tts/generate" `
  -Method POST -ContentType "application/json" `
  -Body '{"text": "Test", "language": "en"}'

# 3. Результат
Write-Host "✓ Success! Audio ID: $($r.audio_id)"
```

---

## 📚 Дополнительная документация

- **TTS_TESTING_STEPS.md** - Полное руководство
- **TTS_QUICK_TEST.md** - Быстрые команды
- **TTS_TESTING_COMPLETE.md** - Обзор всех тестов

---

**Готово!** 🎉
